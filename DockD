public class DockD extends Dock {
    public DockD(boolean isDockEmpty, String position) {
        super(isDockEmpty, position);
    }

    @Override
    public void addVehicle(Vehicle vehicle) {
        super.addVehicle(vehicle);
        System.out.println("Vehicle registered for Dock D");
    }

    @Override
    public void printVehicle() {
        setPosition("D");
        super.printVehicle();
    }
}
