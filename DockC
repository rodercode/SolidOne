public class DockC extends Dock {
    public DockC(boolean isDockEmpty, String position) {
        super(isDockEmpty, position);
    }

    @Override
    public void addVehicle(Vehicle vehicle) {
        super.addVehicle(vehicle);
        System.out.println("Vehicle registered for Dock C");
    }

    @Override
    public void printVehicle() {
        setPosition("C");
        super.printVehicle();
    }
}
